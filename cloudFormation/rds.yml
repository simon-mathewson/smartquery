AWSTemplateFormatVersion: '2010-09-09'
Description: 'RDS PostgreSQL database for Dabase Cloud'

Parameters:
  DabaseCloudDbSecretArn:
    Type: String

Resources:
  DBSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupDescription: 'Subnet group for RDS PostgreSQL'
      SubnetIds: !Split [',', !ImportValue DabaseCloudPrivateSubnets]

  DBInstance:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      DBName: dabase_cloud
      Engine: postgres
      MasterUsername: !Sub '{{resolve:secretsmanager:${DabaseCloudDbSecretArn}::username}}'
      MasterUserPassword: !Sub '{{resolve:secretsmanager:${DabaseCloudDbSecretArn}::password}}'
      DBInstanceClass: db.t3.micro
      AllocatedStorage: 20
      MultiAZ: false
      PubliclyAccessible: false
      VPCSecurityGroups:
        - !ImportValue DabaseCloudDbSecurityGroup
      DBSubnetGroupName: !Ref DBSubnetGroup
      BackupRetentionPeriod: 7
      PreferredBackupWindow: '03:00-04:00'
      PreferredMaintenanceWindow: 'sun:04:00-sun:05:00'
      StorageEncrypted: true

  # Populates secret with connection info
  DBInstanceSecretAttachment:
    Type: AWS::SecretsManager::SecretTargetAttachment
    Properties:
      SecretId: !ImportValue DabaseCloudDbSecretArn
      TargetId: !Ref DBInstance
      TargetType: AWS::RDS::DBInstance

Outputs:
  DBEndpoint:
    Description: 'Database endpoint'
    Value: !GetAtt DBInstance.Endpoint.Address
    Export:
      Name: DabaseCloudDbEndpoint
